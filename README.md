# La Jolie Nail

V1 do sistema de gestão para nail designer.

Stack: React + TypeScript + Vite + Supabase + PWA + Lucide React.

## Rodar

```bash
npm.cmd install
npm.cmd run dev
```

## Build

```bash
npm.cmd run build
```
